# 📖 توثيق بوت IKON STORE الشامل

## 🎯 نظرة عامة

هذا البوت يعمل كوسيط بين المستخدمين وبوتات Steam للحصول على رموز التحقق. يدعم ثلاثة مصادر مختلفة:

1. **@hllestore_bot** (7LE STORE) - البوت الافتراضي
2. **@PoweredSteamBot** - لقائمة حسابات محددة (القائمة العادية)
3. **@PoweredSteamBot** - لقائمة حسابات RE9 (برقم طلب مختلف)

---

## 📁 هيكل الملفات

```
├── bot.py          # الملف الرئيسي للبوت
├── db.py           # اتصال قاعدة البيانات
├── init_db.py      # إنشاء الجداول
├── requirements.txt # المكتبات المطلوبة
├── Procfile        # للنشر على Heroku/Railway
└── BOT_DOCUMENTATION.md  # هذا الملف
```

---

## 🔧 متغيرات البيئة المطلوبة

| المتغير | الوصف |
|---------|-------|
| `API_ID` | معرف التطبيق من Telegram |
| `API_HASH` | هاش التطبيق من Telegram |
| `SESSION_STRING` | جلسة Userbot (للتواصل مع البوتات الأخرى) |
| `BOT_TOKEN` | توكن البوت الخاص بك |
| `DATABASE_URL` | رابط قاعدة بيانات PostgreSQL |

---

## 📋 القوائم الثلاث للحسابات

### 1️⃣ قائمة hllestore_bot (الافتراضية)
- **المتغير:** لا يوجد قائمة محددة (أي حساب غير موجود في القوائم الأخرى)
- **رقم الطلب:** `208718912` (ثابت `FIXED_ORDER_NUMBER`)
- **البوت:** `@hllestore_bot`
- **آلية العمل:**
  1. المستخدم يرسل اسم الحساب
  2. البوت يرسل اسم الحساب لـ hllestore_bot
  3. hllestore_bot يطلب رقم الطلب → نرسله تلقائياً
  4. hllestore_bot يطلب اسم الحساب → نرسله تلقائياً
  5. ننتظر رمز التحقق أو رسالة تسجيل الدخول

### 2️⃣ قائمة PoweredSteamBot العادية
- **المتغير:** `POWERED_STEAM_ACCOUNTS`
- **رقم الطلب:** `242549795` (`POWERED_STEAM_ORDER_NUMBER`)
- **البوت:** `@PoweredSteamBot`
- **عدد الحسابات:** ~115 حساب
- **آلية العمل:**
  1. المستخدم يرسل اسم حساب من القائمة
  2. إذا كانت آخر عملية من نفس القائمة → نرسل اسم الحساب مباشرة
  3. إذا كانت آخر عملية من قائمة مختلفة → نرسل رقم الطلب أولاً
  4. PoweredSteamBot يرد "ممتاز..." → نرسل اسم الحساب
  5. ننتظر رمز التحقق

### 3️⃣ قائمة RE9
- **المتغير:** `RE9_ACCOUNTS`
- **رقم الطلب:** `242875026` (`RE9_ORDER_NUMBER`)
- **البوت:** `@PoweredSteamBot`
- **عدد الحسابات:** 19 حساب
- **الحسابات:**
  ```
  cpcnc993712, yf1qf5oh4xp0, qp6ef1dc6av3, ur8ts3zk4kf0, sf6mv9vq9gv8,
  gc9um3qb1sh6, bb1hr4yz8gs8, vt1ot6nt3ro2, de8mg8qz0yo8, ax8az1rv1ga7,
  tw5rm2tz3am3, ke6gc9yu6ij5, prmst44110, prtzt73337, prvnd57593,
  qm5vq5yj2zc4, gk0lm5xr0nf1, mv8ue9ak9un4, lb2ef6wz4ml8
  ```
- **آلية العمل:** نفس القائمة العادية لكن برقم طلب مختلف

---

## 🔄 آلية تتبع القوائم

### المتغير `last_powered_list_type`
يتتبع آخر قائمة استُخدمت مع PoweredSteamBot:
- `'powered'` = آخر عملية كانت من القائمة العادية
- `'powered_re9'` = آخر عملية كانت من قائمة RE9
- `None` = لم تُستخدم بعد

### السيناريوهات:
| آخر قائمة | الحساب الجديد | الإجراء |
|-----------|---------------|---------|
| `None` | عادية | إرسال رقم طلب 242549795 + انتظار رد + اسم الحساب |
| `None` | RE9 | إرسال رقم طلب 242875026 + انتظار رد + اسم الحساب |
| `powered` | عادية | إرسال اسم الحساب مباشرة (بدون رقم طلب) |
| `powered` | RE9 | إرسال رقم طلب 242875026 + انتظار رد + اسم الحساب |
| `powered_re9` | RE9 | إرسال اسم الحساب مباشرة (بدون رقم طلب) |
| `powered_re9` | عادية | إرسال رقم طلب 242549795 + انتظار رد + اسم الحساب |

---

## 🔑 المتغيرات الرئيسية في الكود

```python
# رقم الطلب لـ hllestore_bot
FIXED_ORDER_NUMBER = '208718912'

# أرقام طلبات PoweredSteamBot
POWERED_STEAM_ORDER_NUMBER = '242549795'  # للقائمة العادية
RE9_ORDER_NUMBER = '242875026'            # لقائمة RE9

# أسماء البوتات
steam_bot_username = 'hllestore_bot'          # البوت الجديد
powered_steam_bot_username = 'PoweredSteamBot' # البوت القديم

# تتبع نوع الطلب لكل مستخدم
request_bot_type = {}  # القيم: 'hlle', 'powered', 'powered_re9'

# تتبع آخر قائمة مستخدمة
last_powered_list_type = None  # القيم: 'powered', 'powered_re9', None
```

---

## 👤 أوامر الأدمن

| الأمر | الوصف |
|-------|-------|
| `/maintenance` | تفعيل/إلغاء وضع الصيانة |
| `/status` | حالة البوت |
| `/orders` | عرض الطلبات |
| `/add رقم1 رقم2` | إضافة طلبات |
| `/ban رقم` | حظر طلب |
| `/unban رقم` | إلغاء حظر |
| `/del رقم` | حذف طلب |
| `/users` | المستخدمين النشطين |
| `/kick user_id` | طرد مستخدم |
| `/broadcast رسالة` | إرسال للجميع |
| `/msg رقم_طلب رسالة` | إرسال لرقم طلب |
| `/stats` | إحصائيات |
| `/logs` | آخر 10 عمليات |
| `/logs رقم_طلب` | عمليات رقم طلب |
| `/userlogs اسم` | عمليات مستخدم |
| `/reset` | إعادة تعيين البوت |

---

## 🛠️ كيفية إضافة حسابات جديدة

### إضافة حساب للقائمة العادية:
في ملف `bot.py`، أضف اسم الحساب (بالحروف الصغيرة) إلى `POWERED_STEAM_ACCOUNTS`:
```python
POWERED_STEAM_ACCOUNTS = {
    ...
    'اسم_الحساب_الجديد',
}
```

### إضافة حساب لقائمة RE9:
أضف اسم الحساب (بالحروف الصغيرة) إلى `RE9_ACCOUNTS`:
```python
RE9_ACCOUNTS = {
    ...
    'اسم_الحساب_الجديد',
}
```

### تغيير رقم طلب:
```python
POWERED_STEAM_ORDER_NUMBER = 'رقم_جديد'  # للقائمة العادية
RE9_ORDER_NUMBER = 'رقم_جديد'            # لقائمة RE9
FIXED_ORDER_NUMBER = 'رقم_جديد'          # لـ hllestore_bot
```

---

## 🔍 Handlers (معالجات الردود)

### 1. `handle_bot_message` 
- يستقبل رسائل المستخدمين
- يتحقق من رقم الطلب والتفعيل
- يوجه الحساب للبوت المناسب

### 2. `handle_steam_reply`
- يعالج ردود `@hllestore_bot`
- يكتشف طلب رقم الطلب ويرسله تلقائياً
- يكتشف طلب اسم الحساب ويرسله تلقائياً
- يرسل رموز التحقق للمستخدم

### 3. `handle_powered_steam_reply`
- يعالج ردود `@PoweredSteamBot`
- يكتشف "ممتاز. الرجاء كتابة اسم حساب" ويرسل اسم الحساب
- يرسل رموز التحقق للمستخدم

---

## ⚠️ ملاحظات مهمة

1. **الحروف الصغيرة:** جميع أسماء الحسابات في القوائم يجب أن تكون بالحروف الصغيرة (lowercase)
2. **المقارنة:** عند المقارنة، يتم تحويل اسم الحساب المُدخل إلى حروف صغيرة
3. **التكرار:** لا تكرار لرقم الطلب إذا كانت العملية من نفس القائمة
4. **المستخدمين المحظورين:** القائمة `BLOCKED_USERNAMES` تحتوي على يوزرات محظورة تماماً

---

## 📝 كيف تخبر الذكاء الاصطناعي عن هذا الملف

عندما تفتح شات جديد، قل:
> "اقرأ ملف BOT_DOCUMENTATION.md في المشروع وافهم آلية عمل البوت ثم ساعدني في [طلبك]"

أو:
> "راجع ملف التوثيق BOT_DOCUMENTATION.md أولاً قبل أي تعديل"

---

## 📅 آخر تحديث
- التاريخ: 28 فبراير 2026
- الإصدار: 2.0
